@model UserUpdateDto
@inject Microsoft.Extensions.Localization.IStringLocalizer<PeerReview.MvcHotel.Resources.SharedResource> L

@{
    ViewData["Title"] = L["Users_Edit_Title"];
    ViewBag.ActiveMenu = "Users";
}

@section HeaderActions {
    <a href="@Url.Action("Index", "Users")" class="btn-pr btn-pr-outline">
        ← @L["Btn_Back"]
    </a>
}

<div class="row justify-content-center">
    <div class="col-lg-7 col-xl-6">
        <div class="pr-card">
            <div class="h-section-title mb-3">
                @L["Users_Edit_Header"]
            </div>

            <form method="post" asp-action="Edit" asp-route-id="@(ViewBag.UserId)">
                @Html.AntiForgeryToken()

                <!-- الاسم الكامل -->
                <div class="pr-field">
                    <input asp-for="fullName" placeholder=" " />
                    <label>@L["Users_Field_FullName"]</label>
                    @Html.ValidationMessageFor(m => m.fullName, "", new { @class = "text-danger small" })
                </div>

                <!-- البريد -->
                <div class="pr-field">
                    <input asp-for="email" placeholder=" " type="email" />
                    <label>@L["Users_Field_Email"]</label>
                    @Html.ValidationMessageFor(m => m.email, "", new { @class = "text-danger small" })
                </div>

                <!-- الدور -->
                <div class="pr-field">
                    <select asp-for="roleId"
                            class="form-select"
                            asp-items="@(new SelectList((IEnumerable<SelectListItem>)ViewBag.Roles, "Value", "Text"))">
                        <option value="">-- @L["Users_Field_SelectRole"] --</option>
                    </select>
                    <label>@L["Users_Field_Role"]</label>
                    @Html.ValidationMessageFor(m => m.roleId, "", new { @class = "text-danger small" })
                </div>

                <!-- الحالة -->
                <div class="form-check form-switch mb-3">
                    @Html.CheckBoxFor(m => m.isActive, new { @class = "form-check-input", id = "isActive" })
                    <label class="form-check-label" for="isActive">@L["Status_Active"]</label>
                </div>

                <div class="d-flex justify-content-end gap-2 mt-3">
                    <a href="@Url.Action("Index", "Users")" class="btn-pr btn-pr-outline">
                        @L["Btn_Back"]
                    </a>
                    <button type="submit" class="btn-pr btn-pr-primary">
                        @L["Btn_Save"]
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>
